services:
  laravel:
    extends:
      file: compose.common.yaml
      service: laravel
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      memcached:
        condition: service_healthy

  mysql:
    extends:
      file: compose.common.yaml
      service: mysql

  redis:
    extends:
      file: compose.common.yaml
      service: redis

  memcached:
    extends:
      file: compose.common.yaml
      service: memcached

volumes:
  mysql:
    driver: local
  redis:
    driver: local
